{
  "name": "@tetherto/mdk-client",
  "version": "0.7.0",
  "description": "MDK protocol client — HRPC (RPC listener) transport for Kernel",
  "main": "index.js",
  "keywords": [
    "tether",
    "mdk",
    "client"
  ],
  "author": "Tether",
  "license": "Apache-2.0",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "lint": "standard",
    "lint:fix": "standard --fix",
    "test": "npm run lint && npm run test:unit",
    "test:unit": "NODE_ENV=test brittle tests/unit/*.js",
    "test:coverage": "NODE_ENV=test brittle --coverage 'tests/**/*.test.js'"
  },
  "dependencies": {
    "@hyperswarm/rpc": "^3.5.0",
    "debug": "^4.4.1",
    "hyperdht": "^6.32.0"
  },
  "devDependencies": {
    "brittle": "3.18.0",
    "standard": "17.1.0"
  },
  "overrides": {
    "brace-expansion": "5.0.9"
  }
}
