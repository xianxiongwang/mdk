{
  "name": "@tetherto/mdk-worker-demo",
  "version": "0.7.0",
  "description": "Third-party plugin-authoring demo Worker Plugin with its own SQLite persistence — an mdk-contract.json plus src/ handler files, loaded from this directory and instantiated per device by the Worker Runtime (see examples/backend/demo-worker-caller); it exports no module of its own",
  "keywords": [
    "tether"
  ],
  "author": "Tether",
  "license": "Apache-2.0",
  "engine": {
    "node": ">=24"
  },
  "scripts": {
    "lint": "standard",
    "lint:fix": "standard --fix",
    "test": "npm run lint && npm run test:unit",
    "test:unit": "NODE_ENV=test brittle tests/unit/*.test.js",
    "test:coverage": "NODE_ENV=test brittle --coverage tests/unit/*.test.js"
  },
  "dependencies": {
    "@tetherto/mdk-worker": "^0.7.0",
    "debug": "4.4.1"
  },
  "devDependencies": {
    "brittle": "3.16.3",
    "standard": "17.1.2"
  },
  "overrides": {
    "brace-expansion": "5.0.9"
  }
}
